# Code Review Brief — ADO#3096, ADO#3107, ADO#3109
## Reviewer: Hawkeye (Clint Barton), Cycle 1

You are performing a thorough code review of three FAIT v2 work items. All were committed together or in sequence. Your job is to read the actual code and verify it against the acceptance criteria.

Working directory: `/home/fredw/projects/fip/fait-v2`

---

## REVIEW 1: ADO#3096 — Scheduled Task Email Notifications

### Files to read
1. `Services/ScheduledTaskNotificationService.cs` (new file)
2. `Program.cs` — find the DI registration for `IScheduledTaskNotificationService` (around line 188)
3. `Services/ScheduledTaskBackgroundService.cs` — find all three fire-and-forget notification calls

### What to verify
1. **Interface + implementation exist** — `IScheduledTaskNotificationService` interface defined, `ScheduledTaskNotificationService` implements it
2. **Namespace** — Should be in `FaitV2` or `FaitV2.Services` namespace
3. **DI registration** — `AddScoped<IScheduledTaskNotificationService, ScheduledTaskNotificationService>()` present in Program.cs
4. **AlertOnCompletion / AlertOnFailure flags** — Before sending email, code must check these flags on the task. Only sends if the appropriate flag is true.
5. **Graph token null check** — If token is null (or GetValidAccessTokenAsync returns null), the code must log a warning and return, NOT throw an exception
6. **Graph email POST** — Uses `https://graph.microsoft.com/v1.0/me/sendMail` endpoint
7. **HTML email body** — Contains task name, run ID, status, and a preview of output (max ~500 chars) and error message if failed
8. **Never throws** — ALL failure paths (null user, null token, non-2xx response, exception) must log+return, never re-throw
9. **Three fire-and-forget calls in ScheduledTaskBackgroundService** — After each of the three outcome paths (TaskMode=true branch success, CC path success, catch/exception block), there must be a `Task.Run(async () => await _notificationService.SendAsync(...))` or equivalent. Verify the exact placement — should be AFTER the final `SaveChangesAsync` call in each branch.
10. **No race conditions** — Variables captured in Task.Run closures should be safe (immutable or not modified after closure starts)
11. **CancellationToken.None** — Notification should use CancellationToken.None, not the background service's stopping token

### Issues to flag
- Critical: Missing null checks, exceptions can propagate, wrong flags checked
- Important: Missing a fire-and-forget branch, wrong token, hardcoded values that should be config
- Nitpick: Naming, log message quality, code style

---

## REVIEW 2: ADO#3107 — G7 Scheduled Task Approval Gate

### Files to read
1. `Data/Models/ScheduledTaskApproval.cs` (new file)
2. `Data/FaitV2DbContext.cs` — find the new DbSet addition
3. `Migrations/20260509075646_AddScheduledTaskApprovals.cs` (or .Designer.cs) — check the migration
4. `Program.cs` — find the two new endpoints: `/api/scheduled-tasks/approval/request` and `/api/scheduled-tasks/approval/respond`
5. `Services/IUserAgentRuntime.cs` — find the `TurnRequest` record and `IsScheduledTask` property
6. `Services/ScheduledTaskBackgroundService.cs` — find where `TurnRequest` is constructed with `IsScheduledTask: true`
7. `agent-harness/harness-server.js` — find `scheduledTaskUsers`, `isScheduledTask` handling in `/turn`, and updated `requireApproval()`

### What to verify

#### DB Model (ScheduledTaskApproval.cs)
- Fields present: id, scheduled_task_id, intervention_id, action_type, action_summary, status, created_at, expires_at, resolved_at
- Status field — should be string or enum: pending/approved/denied/expired
- expires_at — should default to 24 hours from created_at (check if this is set in the endpoint or the model)

#### DbContext
- `DbSet<ScheduledTaskApproval> ScheduledTaskApprovals` property added

#### Migration
- Creates `scheduled_task_approvals` table
- All columns present with appropriate types
- No orphaned or missing columns

#### Program.cs Endpoints

**POST /api/scheduled-tasks/approval/request:**
- Guarded by X-Internal-Token check (not just auth middleware — must explicitly check the token header)
- Creates a new `ScheduledTaskApproval` record in the DB with status="pending"
- Sets expires_at to now + 24h
- Sends Graph email notification if userId is provided (subject contains "Approval Required")
- Returns appropriate response (201 or 200 with the approval ID)

**POST /api/scheduled-tasks/approval/respond:**
- Guarded by cookie auth (not X-Internal-Token)
- Looks up the approval record by ID
- Validates: record must exist, status must be "pending", must not be expired (check expires_at vs. now)
- Sets status to approved or denied
- Sets resolved_at to now
- Returns appropriate response
- If expired: returns error response (not 200)
- Ownership check: does it verify the responding user owns the task? (Flag as observation if missing)

#### IUserAgentRuntime.cs
- `TurnRequest` record has `bool IsScheduledTask { get; init; } = false` (or similar default-false property)

#### ScheduledTaskBackgroundService.cs
- `TurnRequest` construction in TaskMode path passes `IsScheduledTask: true`
- NOT passed in the non-scheduled-task path (or defaults to false)

#### harness-server.js
- `const scheduledTaskUsers = new Set()` declared at module level (not inside a function)
- `/turn` handler reads `isScheduledTask` from `req.body`
- If `isScheduledTask === true`: adds `userId` to `scheduledTaskUsers`
- If `isScheduledTask !== true` (or at turn end): removes `userId` from `scheduledTaskUsers`
- Cleanup: userId removed from Set in ALL completion/error paths (CC close, Bedrock stream end, error handlers) — check for completeness
- `requireApproval()` function:
  - Checks `scheduledTaskUsers.has(userId)` first
  - If yes: POSTs to `/api/scheduled-tasks/approval/request` with relevant data, then returns `false` immediately (or `{ denied: true, reason: ... }`)
  - Falls through to existing G2 SignalR path if not a scheduled task user
  - The POST to approval/request should include: userId, intervention_id, action_type, action_summary

### Issues to flag
- Critical: Missing cleanup paths in harness (memory leak if userId never removed), missing X-Internal-Token guard, status machine not enforced
- Important: `ScheduledTaskId` empty string (known, flag as observation), ownership check missing on respond endpoint (flag as observation)
- Nitpick: naming, log quality

---

## REVIEW 3: ADO#3109 — G4: MCP Tool Allowlist Enforcement

### Files to read
1. `agent-harness/harness-server.js` — find `MCP_TOOL_ALLOWLIST`, `BUILTIN_TOOLS`, `isToolAllowed()`, and the generic `/tools/:toolName` handler

### What to verify

1. **MCP_TOOL_ALLOWLIST map** — Must have entries for at minimum: graph server tools (graph_list_emails, graph_send_email, etc.), ado server tools (ado_get_work_item, ado_update_work_item, etc.), and stitch server tools (generate_screen_from_text, etc.)
2. **BUILTIN_TOOLS Set** — Must contain at minimum `list_workspace_files` and `search_memory`
3. **isToolAllowed() function** — Must check both `MCP_TOOL_ALLOWLIST` (checking if toolName is a value, or if it's a key) and `BUILTIN_TOOLS`
4. **Generic `/tools/:toolName` catch-all** — Must have `if (!isToolAllowed(toolName)) { return res.status(403).json(...) }` BEFORE calling the Stitch handler
5. **Check ordering in the generic handler** — The G4 allowlist check must fire AFTER the ADO#3101 server-permission check and ADO#3106 KB-write check. Read the code to verify the order of these checks.
6. **Named routes still work** — Named routes like `/tools/graph_list_emails` are registered separately and bypass the catch-all. The `isToolAllowed` check is ONLY in the catch-all. Verify this is the case and that named routes don't accidentally get double-checked.
7. **STITCH_TOOLS overlap** — There's a note that `MCP_TOOL_ALLOWLIST` stitch entries mirror `STITCH_TOOLS`. Verify both exist and that the Stitch dispatch guard inside the generic handler still works correctly after the G4 check is added.
8. **node --check** — Already confirmed by Tony, but note this

### Issues to flag
- Critical: If allowlist check fires BEFORE required permission checks (wrong order), if allowlisted tools get blocked, if the check can be bypassed
- Important: If BUILTIN_TOOLS is missing, if the map structure is wrong (using wrong lookup method)
- Nitpick: Code organization, naming

---

## Review Output Instructions

For each WI, provide:

1. **Verdict:** PASS / NEEDS-CHANGES / FAIL
2. **Critical issues** (blocking — must fix before ship)
3. **Important issues** (should fix)
4. **Nitpick issues** (optional cleanup)
5. **Observations** (non-blocking notes, e.g. known edge cases flagged by Tony)

Be specific: cite file, function name, line number if visible. Quote the code if relevant.

Do NOT rubber-stamp. If code is actually correct, say so with evidence. If something is wrong, show exactly what's wrong and what the fix should be.

Focus particularly on:
- Null safety and exception handling
- State machine correctness (especially the approval endpoint status transitions)
- Cleanup completeness in the harness (Set membership, no leaks)
- Security: X-Internal-Token guard, auth on approval respond
- Check ordering (G4 must not fire before G2/G3 checks)
