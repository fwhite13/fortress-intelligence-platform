# QA Report — ADO#3298/#3299/#3300/#3301
**Date:** 2026-05-12  
**QA:** Natasha (Black Widow)  
**Scope:** ECS deployed state + code verification at HEAD `1ed77b5b`

---

## Verdict: ✅ PASS

---

## Checks

| # | Check | Result | Notes |
|---|-------|--------|-------|
| ECS-1 | fred-dev:191 running | ✅ PASS | `status: ACTIVE`, `runningCount: 1`, task def `fred-dev:191` confirmed |
| ECS-2 | harness:29 image = `1ed77b5b` | ✅ PASS | `742932328420.dkr.ecr.us-east-1.amazonaws.com/fait-v2-agent-harness:1ed77b5b` |
| ECS-3 | `INTERNAL_API_TOKEN` aligned | ✅ PASS | `TOKEN: MATCH` — harness:29 and fred-dev:191 share identical token |
| ECS-4 | `Fargate__TaskDefinition` → harness:29 | ✅ PASS | `arn:aws:ecs:us-east-1:742932328420:task-definition/fait-v2-agent-harness:29` |
| #3298 | Non-root user + `/workspace` chown in Dockerfile | ✅ PASS | Line 51: `chown -R harness:harness /app`; Line 54: `mkdir -p /workspace && chown harness:harness /workspace`; Line 63: `USER harness` |
| #3299 | `getUserTokens` called unconditionally on every `/turn` | ✅ PASS | Line 1531 comment confirms ADO#3299 intent; line 1533: unconditional call; line 1534: success log with `ms365`/`ado` flags |
| #3300 | `TeamKbEnabled` uses `hasTeamKb` only (NOT `\|\| hasProjectKb`) | ✅ PASS | `TeamKbEnabled: hasTeamKb` (line 915) — isolated, no `hasProjectKb` contamination. `hasTeamKb` defined as `_selectedTeamIds?.Any() == true` (line 833) |
| #3301 | `list_files` uses Blazor API, not MySQL | ✅ PASS | Handler calls `fetch(${blazorBase}/api/workspace/internal/list-files, ...)` (line 1014). `getDbConnection()` at line 1042 is inside the separate `read_file` handler — no MySQL in `list_files` |

---

## Detail Notes

### ECS State
- **fred-dev:191** — ACTIVE, 1 task running
- **harness:29** — image tag `1ed77b5b` confirmed; task def ARN registered as `Fargate__TaskDefinition` in fred-dev:191
- **Token alignment** — both services share the same `INTERNAL_API_TOKEN` ✅

### #3298 — Non-root container security
Dockerfile correctly: creates `harness` user, chowns `/app` and `/workspace`, then drops to `USER harness` before the entrypoint. Pattern is correct.

### #3299 — getUserTokens unconditional call
Comment at line 1531 explicitly documents the ADO#3299 requirement. The call at line 1533 is outside any conditional block. Success log at line 1534 confirms both `ms365` and `ado` token presence is logged per turn.

### #3300 — KbFlags TeamKbEnabled isolation
`hasTeamKb` (line 833) is independently derived from `_selectedTeamIds`. `TeamKbEnabled` in the `KbFlags` constructor (line 915) passes `hasTeamKb` directly. There is no `|| hasProjectKb` expression anywhere in the flag assignment chain. The flags are correctly isolated.

### #3301 — list_files uses Blazor API
The `list_files` tool handler exclusively uses `fetch()` against the Blazor internal API endpoint. The `getDbConnection()` function exists in the file but is only called in the `read_file` handler (a separate `app.post` block). No MySQL dependency in the `list_files` path.

---

## Verdict Summary

All 8 checks pass. Deployed state is consistent with expected code at `1ed77b5b`. No blockers, no warnings.

**PASS — Ready to proceed.**
