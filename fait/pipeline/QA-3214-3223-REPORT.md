# QA Report: ADO#3214 + ADO#3223

**QA Analyst:** Natasha Romanoff (Black Widow)  
**Date:** 2026-05-10  
**Session:** natasha-qa-3214-3223

---

## Overall Verdict: ✅ PASS (both WIs)

---

## ADO#3214 — Resumption Brief ProtectedSessionStorage Guard

### Verdict: ✅ PASS

### Acceptance Criteria Results

**AC1 — `ProtectedSessionStorage` injected in Chat component**  
✅ PASS  
`ChatView.razor` line 29:
```razor
@inject Microsoft.AspNetCore.Components.Server.ProtectedBrowserStorage.ProtectedSessionStorage SessionStorage
```

**AC2 — Resumption brief logic checks session storage before firing**  
✅ PASS  
`HandleAgentReady()` (lines ~1452–1490) reads `SessionStorage.GetAsync<string>("resumption_brief_session")` before deciding whether to fire. If stored session ID matches current session ID, `_resumptionBriefSent = true` and returns early — brief does NOT fire.

**AC3 — Key is derived from `RuntimeSession.SessionId` (not a fixed string)**  
✅ PASS  
Guard logic calls `AgentRuntime.GetSessionAsync(Session.UserId.ToString())` and reads `session?.SessionId` as the key value. The storage key is the dynamic `SessionId`, not a hardcoded constant. The storage slot itself uses the name `"resumption_brief_session"` but the *value* compared is the live `SessionId`.

**AC4 — Build health (no startup errors)**  
✅ PASS  
CloudWatch log stream `ecs/fred/ad5766d1df6149e695a7f5a187352b62` shows clean startup:
```
Now listening on: http://[::]:8080
Application started. Press Ctrl+C to shut down.
```
Only EF `DbCommand` failures present are idempotent migration probes (column-already-exists checks) — **expected baseline behavior**, not related to this WI.

**AC5 — `fred-dev:174` running HEALTHY, correct commit in chain**  
✅ PASS  
```json
{
  "status": "ACTIVE",
  "taskDef": "arn:aws:ecs:us-east-1:742932328420:task-definition/fred-dev:174",
  "running": 1,
  "desired": 1
}
```
Image: `fred-chat:3b7415a3` — this is the exact commit for the ProtectedSessionStorage fix (`fix(fait#3214): prevent resumption brief re-firing on /chat nav`). Confirmed via `git log --oneline`.

---

## ADO#3223 — Harness BLAZOR_BASE_URL → FAIT_BASE_URL

### Verdict: ✅ PASS

### Acceptance Criteria Results

**AC1 — `search_memory`, `read_memory`, `write_memory` all use `FAIT_BASE_URL`**  
✅ PASS  
`harness-server.js` — all three handlers use `FAIT_BASE_URL`:
- Line 76: `const FAIT_BASE_URL = process.env.FAIT_BASE_URL || 'http://localhost:8080';`
- Line 681 (`search_memory`): `const blazorBase = FAIT_BASE_URL;`
- Line 711 (`read_memory`): `const blazorBase = FAIT_BASE_URL;`
- Line 745 (`write_memory`): `const blazorBase = FAIT_BASE_URL;`

**AC2 — No other handlers still reference `BLAZOR_BASE_URL`**  
✅ PASS  
`grep -n "BLAZOR_BASE_URL" harness-server.js` → exit code 1, zero matches. `BLAZOR_BASE_URL` is completely absent from the file.

**AC3 — `fred-dev:174` ACTIVE / HEALTHY, correct image digest**  
✅ PASS (shared with #3214 AC5 above)  
Status: ACTIVE, running: 1/1, image: `fred-chat:3b7415a3`

**AC4 — `fait-v2-agent-harness:15` registered with correct commit tag**  
✅ PASS  
Task definition `fait-v2-agent-harness:15`:
- Image: `fait-v2-agent-harness:42be3a3f`
- `FAIT_BASE_URL` env var: `https://fait.dev.fortressam.ai` ✅
- `42be3a3f` is the commit immediately after the harness fix in the repo — confirmed in chain via git log

---

## Service Health Summary

| Service | Task Def | Status | Running | Image |
|---------|----------|--------|---------|-------|
| fred-dev | :174 | ACTIVE | 1/1 | fred-chat:3b7415a3 |
| fait-v2-agent-harness | :15 | Registered | on-demand | fait-v2-agent-harness:42be3a3f |

---

## CloudWatch

Log group: `/ecs/fred-dev`  
Most recent stream: `ecs/fred/ad5766d1df6149e695a7f5a187352b62`  
**Result:** Clean startup. No errors related to either WI. Idempotent migration probes present — expected baseline behavior.

---

## Browser E2E

⚠️ **Blocked** — Pre-existing Cloudflare/TestAuth constraint prevents authenticated browser QA against `https://fait.dev.fortressam.ai` from this headless environment. This constraint is pre-existing and not introduced by either WI.

Both WIs are Blazor/harness-only changes with no new UI surfaces to visually validate. Code-level verification + ECS health + CloudWatch startup inspection is the correct QA method for these WIs.

---

## Test Duration

~8 minutes (code verification + AWS health checks + CloudWatch analysis)

---

## Notes

- The harness `42be3a3f` image tag appears to be a docs/build-report commit layered on top of the `3b7415a3` fix commit in the same branch — both WIs' code changes are in the deployed artifacts.
- No rollback indicators. Both WIs are safe to close.
