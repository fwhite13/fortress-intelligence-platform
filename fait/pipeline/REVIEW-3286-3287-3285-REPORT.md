# Code Review Report — ADO#3286 + #3287 + #3285
**Review cycle:** 1 of 2
**HEAD:** 5518e0e6
**Reviewer:** Clint (pipeline agent)
**Date:** 2026-05-11
**Verdict: PASS**

---

## CC Invocation
```
cat /tmp/review-3286-3287-3285.md | ./scripts/run-cc.sh
```
Files reviewed directly:
- `/home/fredw/projects/fip/fait-v2/agent-harness/harness-server.js`
- `/home/fredw/projects/fip/fait/src/FortressAI.Web/Components/Chat/ChatView.razor`
- `/home/fredw/projects/fip/fait/src/FortressAI.Shared/Models/KbTeam.cs`

---

## ADO#3286 — harness-server.js (commit 35eba9e4)

### Issue A — getUserTokens userId normalization
- **PASS** — `normalizedUserId = (userId || '').trim().toLowerCase()` at line 37, BEFORE the fetch call at line 44. Order is correct.
- **PASS** — Empty userId guard at lines 38–41: returns `{ ms365: null, ado: null }`, matching the catch-block return shape. Callers that do `userTokens.ms365 ?? null` are safe.

### Issue B — Brave proxy URL fix
- **PASS** — Line 1241: `const braveLocalUrl = \`${FAIT_BASE_URL}/internal/mcp/brave\`` — `FAIT_BASE_URL` used correctly.
- **PASS** — `blazorPort` has **zero remaining references** in the file (grep confirmed no matches).
- **PASS** — `FAIT_BASE_URL` defined at line 63 as `process.env.FAIT_BASE_URL || 'http://localhost:8080'`. In Fargate this env var is set to `http://fait.fip.internal:8080`; the `localhost:8080` fallback only affects local dev where harness and Blazor share the same process.
- **PASS** — All other `localhost:${PORT}` references at lines 2212–2385 are the harness calling its **own** internal `/tools/*` endpoints (loopback to itself), not Blazor. These are correct and should NOT use `FAIT_BASE_URL`.

---

## ADO#3287 — ChatView.razor (commit 61b4bb81)

### Issue A — KB chip visibility during streaming
- **PASS** — Line 128: `@if ((messages.Any(m => m.Role == "assistant") || isStreaming) && _lastKbResult != null)`
- **PASS** — Field name is `isStreaming` (line 396: `private bool isStreaming = false;`) — lowercase, no underscore. Matches template reference exactly.
- **PASS** — `_lastKbResult != null` guard is still present. When `isStreaming=true` but no KB results have arrived yet (`_lastKbResult == null`), the chip does not render. Chip only shows once `_lastKbResult` is populated via the `kb_sources` SSE event. `_lastKbResult` is reset to `null` before each send (line 730), so there is no stale-result scenario.

### Issue B — TeamIds membership intersection
- **PASS** — Line 917–918: `_selectedTeamIds.Intersect(_userTeams.Select(t => t.Id)).ToList()`
- **PASS** — `KbTeam.Id` is `public int Id { get; set; }` (confirmed in `KbTeam.cs:5`). `_selectedTeamIds` is `HashSet<int>` (line 417). Types are identical — no implicit conversion.
- **PASS** — `_userTeams` is populated in `OnParametersSetAsync` (lines 581–585), guarded by `!_userTeams.Any()` so it is loaded once and cached. The Send button is only accessible after the component has fully rendered, meaning `OnParametersSetAsync` has completed and `_userTeams` is populated (or silently failed).
- **PASS** — If `_userTeams` is empty (user has no teams or load failed), `_selectedTeamIds` will also be empty (users can't select teams that don't exist in the UI). The intersection returns empty; `hasTeamKb = _selectedTeamIds?.Any() == true` evaluates false; `TeamIds` is `null` in `KbFlags`. Fail-closed behavior confirmed.

---

## ADO#3285 — ChatView.razor (commit 5518e0e6)

- **PASS** — Line 604: `_wasColdStart = _agentReady;` is inside the `if (ConversationId != _lastConversationId)` block (lines 595–607). It fires only on cross-chat navigation, not every render cycle.
- **PASS** — `_agentReady` is `private bool _agentReady = false` (line 468). Logic is correct: if agent is already up when user navigates to a different conversation, `_wasColdStart = true` immediately triggers a brief on the next `OnAfterRenderAsync`. If agent is not yet ready, `_wasColdStart = false` here but `HandleAgentReady()` will set it to `true` when the agent comes up — existing path unchanged.
- **PASS** — `HandleAgentReady()` guard at lines 1547–1552: `if (_resumptionBriefSent) { ... return; }` is still present and intact. Not removed.
- **PASS** — Session storage key at lines 1528 and 1570: `$"resumption_brief_{sessionId}_{ConversationId}"` — conversation-scoped. Different conversations get distinct keys; brief is not deduplicated across conversations.

---

## Summary

| ADO | Issue | Result |
|-----|-------|--------|
| #3286 | getUserTokens normalization order + empty guard | PASS |
| #3286 | Brave proxy URL (blazorPort removal, FAIT_BASE_URL) | PASS |
| #3287 | KB chip visibility with isStreaming flag | PASS |
| #3287 | TeamIds intersection type match + fail-closed | PASS |
| #3285 | _wasColdStart placement inside conversation-change guard | PASS |
| #3285 | HandleAgentReady _resumptionBriefSent guard preserved | PASS |
| #3285 | Session storage key is conversation-scoped | PASS |

**Verdict: PASS — no blockers. Safe to ship.**
