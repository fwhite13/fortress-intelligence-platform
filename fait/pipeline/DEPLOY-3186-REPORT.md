# Deploy Report: ADO#3186 — 4.1-A: memory_topics + IMemoryFileService

**Date:** 2026-05-10  
**Agent:** Rhodey (War Machine) — DevOps  
**Service:** `fred-dev` (ECS Fargate)  
**WI:** ADO#3186

---

## Summary

✅ **DB Migration applied** — `memory_topics` table verified in `fait_dev`  
✅ **Docker image built** — `fred-chat:72bc61af` (HEAD at deploy time)  
✅ **ECR push successful** — digest `sha256:6c5db45d47948bb5b04bae1daee5d818e7db5f343de55b53578d0e3bf2bd0444`  
✅ **Task def registered** — `fred-dev:162`  
✅ **ECS service updated** — `fred-dev` → `fred-dev:162`  
✅ **Service stable** — desired=1, running=1, pending=0  

---

## Step-by-Step Results

| Step | Action | Result |
|------|--------|--------|
| 1 | Verify commit + push to origin | ✅ HEAD `72bc61af` pushed (main was 1 ahead) |
| 2 | Apply DB migration | ✅ `memory_topics` table already existed (applied earlier); `__EFMigrationsHistory` entry inserted |
| 3 | Docker build (`fait/Dockerfile.debian`, `--no-cache`) | ✅ Built successfully |
| 4 | ECR login + tag + push | ✅ Pushed to `fred-chat:72bc61af` |
| 5 | Clone task def from `:161` + update image | ✅ Saved as `/tmp/fred-dev-taskdef-3186-new.json` |
| 6 | Register new task def | ✅ `fred-dev:162` |
| 7 | Update ECS service | ✅ Service set to `fred-dev:162` |
| 8 | Wait for stability | ✅ Service stable |
| 9 | Final service verification | ✅ ACTIVE, running=1, pending=0 |

---

## DB Migration Details

**Migration:** `20260510144114_AddMemoryTopics`  
**Status:** Table was present (created via EF Core apply at build time despite note about local MySQL unavailability — or prior run). `__EFMigrationsHistory` entry was missing; inserted via `INSERT IGNORE`.

**Note on SQL file:** The migration SQL (`MIGRATION-3186-SQL.sql`) used `CREATE UNIQUE INDEX IF NOT EXISTS` syntax which failed on Aurora MySQL 8.0.40 (syntax not supported on unique indexes). The table and index were already present; only the history entry was missing. Applied manually.

### `SHOW CREATE TABLE memory_topics`
```sql
CREATE TABLE `memory_topics` (
  `id` varchar(36) CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci NOT NULL,
  `user_id` char(36) CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci NOT NULL,
  `topic_name` varchar(200) CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci NOT NULL,
  `topic_slug` varchar(200) CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci NOT NULL,
  `blob_path` varchar(500) CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci NOT NULL,
  `last_updated_at` datetime(6) NOT NULL,
  `created_at` datetime(6) NOT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `ix_memory_topics_user_slug` (`user_id`,`topic_slug`),
  KEY `ix_memory_topics_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
```

---

## Image Details

| Field | Value |
|-------|-------|
| Commit SHA (short) | `72bc61af` |
| Commit SHA (full) | `72bc61af0023103ce1f817659f71dbab68da7814` |
| Commit message | `docs(fait#3186): cycle 2 build report — targeted fixes` |
| Image tag | `fred-chat:72bc61af` |
| ECR URI | `742932328420.dkr.ecr.us-east-1.amazonaws.com/fred-chat:72bc61af` |
| ECR digest | `sha256:6c5db45d47948bb5b04bae1daee5d818e7db5f343de55b53578d0e3bf2bd0444` |

> **Note:** Assignment specified tag `fred-chat:3a93b4c4` but HEAD at deploy time was `72bc61af` (docs commit added after cycle 2 build report). Used actual HEAD per SOUL.md rule: "Always verify the deployed image matches the latest commit."

---

## Task Definition

| | Value |
|-|-------|
| Previous | `fred-dev:161` (commit `385f5692`, ADO#3177) |
| New | `fred-dev:162` (commit `72bc61af`, ADO#3186) |

---

## Final Service Verification

```json
{
  "status": "ACTIVE",
  "taskDef": "arn:aws:ecs:us-east-1:742932328420:task-definition/fred-dev:162",
  "desired": 1,
  "running": 1,
  "pending": 0
}
```

---

## Rollback Plan

If rollback needed:
```bash
aws ecs update-service --cluster fortress-tools-cluster --service fred-dev \
  --task-definition fred-dev:161 \
  --profile fortress-tools-deployer --region us-east-1
```

**DB safety:** Migration is additive (new table + index only, no existing table changes). DB rollback not required if ECS rolls back.

---

## Known Issues / Notes

1. **`CREATE UNIQUE INDEX IF NOT EXISTS` syntax error** in `MIGRATION-3186-SQL.sql` — Aurora MySQL 8.0.40 doesn't support this for unique indexes. The table was already created correctly by the EF runtime. The SQL file should be updated to use a conditional workaround for future runs.
2. **Image tag mismatch:** Assignment said `3a93b4c4` but HEAD was `72bc61af`. Used actual HEAD.

---

*Report generated by Rhodey (War Machine) — DevOps Agent*
