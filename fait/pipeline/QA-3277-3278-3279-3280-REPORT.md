# QA Report: ADO#3277 + #3278 + #3279 + #3280

## QA Verdict: ⚠️ PASS-WITH-NOTES

**Analyst:** Natasha Romanoff (Black Widow)
**Date:** 2026-05-11 14:42–14:47 EDT
**Deployment:** `fred-dev:186` / `fait-v2-agent-harness:24` (commit `0ca05fe8`)
**URL:** https://fait.dev.fortressam.ai
**Test Type:** Code-level + ECS infrastructure (browser blocked by pre-existing Cloudflare managed challenge)

---

## Environment

- **ECS Cluster:** fortress-tools-cluster
- **fred-dev task:** `fred-dev:186` → image `fred-chat:c984fdb0` (Blazor app)
- **Harness task def:** `fait-v2-agent-harness:24` → image `fait-v2-agent-harness:0ca05fe8` (commit `0ca05fe8` — harness changes)
- **Task started:** 2026-05-11 14:39:05 EDT (today — correct)
- **Browser:** Blocked by Cloudflare Managed Challenge (pre-existing — all previous sessions same behavior)

---

## Smoke Tests

| Test | Result | Notes |
|------|--------|-------|
| ECS service healthy | ✅ PASS | `fred-dev` ACTIVE, 1/1 running, HEALTHY |
| Correct task def deployed | ✅ PASS | `fred-dev:186` confirmed PRIMARY |
| Harness image correct | ✅ PASS | `fait-v2-agent-harness:24` → `0ca05fe8` |
| Blazor app started | ✅ PASS | `Application started` / listening on `http://[::]:8080` |
| DB initialization | ✅ PASS | All tables ensured, migrations idempotent, no fatal errors |
| MCP servers seeded | ✅ PASS | devops ✅, m365 ✅ |
| BraveSearch MCP | ⚠️ WARN | 401 on ListTools — BraveSearch key not active in this task (pre-existing, non-blocking) |
| No fatal startup errors | ✅ PASS | Zero `InvalidOperationException`, zero DI failures |
| Browser access | ⚠️ BLOCKED | Cloudflare Managed Challenge blocks headless Chrome + curl (pre-existing) |

---

## Code Verification (Targeted — per WI)

### #3277 — Resumption Brief Fires Once Per Conversation

**File:** `ChatView.razor`

| Check | Result | Evidence |
|-------|--------|---------|
| `_resumptionBriefSent` field exists | ✅ | Line 471: `private bool _resumptionBriefSent = false;` |
| `HandleAgentReady()` early-return when `_resumptionBriefSent` is true | ✅ | Lines 1545–1549: `if (_resumptionBriefSent) { Logger.LogDebug("...skipping"); return; }` |
| Brief resets when switching conversations | ✅ | Line 603: `_resumptionBriefSent = false;` inside conversation-switch handler |
| Session-storage cross-navigation guard | ✅ | Lines 1567–1582: reads `resumption_brief_{sessionId}_{convId}` from ProtectedSessionStorage; sets `_resumptionBriefSent = true` and returns if already stored |
| `_wasColdStart` / `_agentReady` gating still intact | ✅ | Line 674: `if (_wasColdStart && _agentReady && !_resumptionBriefSent)` |

**Verdict: ✅ PASS** — Double-fire bug fixed. Brief fires once on cold navigation, skipped on return to same harness session.

---

### #3278 — KB Auth Scoping / Data Isolation

**Files:** `IUserAgentRuntime.cs`, `ChatView.razor`, `harness-server.js`

| Check | Result | Evidence |
|-------|--------|---------|
| `KbFlags` record has `PersonalKbUserId` field | ✅ | `IUserAgentRuntime.cs` line 64–70: `public record KbFlags(..., string? PersonalKbUserId = null, List<int>? TeamIds = null)` |
| ChatView populates `PersonalKbUserId` with user GUID | ✅ | Line 915: `PersonalKbUserId: hasPersonalKb ? Session.UserId.ToString() : null` |
| ChatView populates `TeamIds` from selected team set | ✅ | Line 916: `TeamIds: hasTeamKb && _selectedTeamIds?.Any() == true ? _selectedTeamIds.ToList() : null` |
| Harness extracts `PersonalKbUserId` from kbFlags | ✅ | Line 1911: `const personalKbUserId = kbFlags.PersonalKbUserId ?? kbFlags.personalKbUserId ?? null;` |
| Personal KB uses `ownerId = userGuid` metadata filter | ✅ | Line 1953: `doKbRetrieval(..., 'ownerId', personalKbUserId)` |
| Team KB uses `teamId` metadata filter per team | ✅ | Lines 1962–1964: one `doKbRetrieval` per `teamId` in `effectiveTeamIds` |
| Fail-closed: Personal KB skipped if `PersonalKbUserId` null | ✅ | Lines 1950–1952: `if (!personalKbUserId) { console.warn(...); }` — skips retrieval |
| Fail-closed: Team KB skipped if `TeamIds` null/empty | ✅ | Lines 1958–1960: `if (!effectiveTeamIds) { console.warn(...); }` — skips retrieval |
| `kb_sources` event only emitted when results exist | ✅ | Line after KB promises: `if (kbSources.length > 0)` → emit; else log "no results, skipping" |

**Verdict: ✅ PASS** — All KB auth scoping correctly implemented. Data isolation enforced at metadata filter level. Fail-closed on null identity fields.

---

### #3279 — RunNow MCP Slug Resolution

**File:** `Tasks.razor`

| Check | Result | Evidence |
|-------|--------|---------|
| `IMcpToolService` injected | ✅ | Line 18: `@inject IMcpToolService McpToolSvc` |
| `RunNowAsync()` calls `McpToolSvc.GetActiveServersForUserAsync(userId)` | ✅ | Lines 522–531: resolves slugs from active server list |
| Slugs built from server `.Slug`, deduplicated | ✅ | `.Select(s => s.Slug).Where(s => !string.IsNullOrEmpty(s)).Distinct().ToList()` |
| `resolvedMcpSlugs` passed to `TurnRequest` as `EnabledMcpSlugs` | ✅ | Line 540: `EnabledMcpSlugs: resolvedMcpSlugs` |
| Graceful fallback if resolution fails | ✅ | Wrapped in `try { } catch { /* best effort — proceed without MCP tools */ }` |

**Verdict: ✅ PASS** — RunNow now mirrors background service behavior for MCP tool availability. `graph_*`, `ado_*` tools will be available in manual task runs.

---

### #3280 — HTML Error Sanitization in Harness

**File:** `harness-server.js`

| Check | Result | Evidence |
|-------|--------|---------|
| `generate-document` error handler detects HTML | ✅ | Lines 944–947: `const isHtml = errText.trim().startsWith('<') \|\| errText.includes('<!DOCTYPE'); const safeErr = isHtml ? 'Document generation failed (HTTP ${genRes.status}). The API returned an unexpected response.' : ...` |
| `read_memory` error handler detects HTML | ✅ | Lines 874–876: `const isHtml = text.trim().startsWith('<') \|\| text.includes('<!DOCTYPE'); const safeText = isHtml ? '[non-JSON response, HTTP ${resp.status}]' : ...` |
| Both return clean error messages (not raw HTML) | ✅ | Both substitute sanitized strings when HTML is detected |

**Verdict: ✅ PASS** — Both error handlers correctly sanitize HTML responses to prevent garbage output to the agent/UI.

---

## Regression Check

| Test | Result | Notes |
|------|--------|-------|
| Application startup clean | ✅ | DB init, MCP seeding, `Application started` — all clean |
| No new DI registration errors | ✅ | Zero `InvalidOperationException` in startup logs |
| Existing MCP tools still registered (devops, m365) | ✅ | `ListTools` succeeded for both (200 OK) |
| KB retrieval logic unchanged for Corp KB | ✅ | Corp KB path has no identity filter (correct — structural scoping) |
| Harness spawning still configured correctly | ✅ | `Fargate__TaskDefinition` = `fait-v2-agent-harness:24` in task env |

---

## Browser Testing

- **Status:** BLOCKED — Cloudflare Managed Challenge on `fait.dev.fortressam.ai`
- **Attempts:** headless Chrome (openclaw profile) → CF 403; curl → CF 403
- **Pre-existing?** Yes — documented in every prior QA session (2026-03-15 onwards)
- **Impact:** Cannot visually verify:
  - Resumption brief card renders once (not twice) — #3277 UI behavior
  - KB source chip absent when no KB results — #3278 UI behavior  
  - No error toast on RunNow dispatch — #3279 UI behavior
  - Normal chat streaming works — regression

**⚠️ All 4 items require Fred manual sign-off.**

---

## Image Tag Note

The deploy brief states commit `0ca05fe8`. This matches the **harness** image (`fait-v2-agent-harness:24` → `0ca05fe8`). The Blazor app (`fred-chat:c984fdb0`) is a pre-existing build. All changes in this batch (#3277–#3280) span:
- **ChatView.razor / Tasks.razor** → compiled into `fred-chat:c984fdb0` ✅ (already deployed)
- **harness-server.js** → compiled into `fait-v2-agent-harness:0ca05fe8` ✅ (confirmed in harness task def)

Both images are live and running.

---

## Acceptance Criteria Status

| Criterion | Status | Notes |
|-----------|--------|-------|
| Chat loads without JS errors | ⚠️ REQUIRES MANUAL | Cloudflare blocks headless — cannot verify |
| Resumption brief fires on cold nav (not every turn) | ✅ PASS (code) / ⚠️ manual for runtime | Early-return guard confirmed in code |
| KB indicator only shows when KB returned actual results | ✅ PASS (code) / ⚠️ manual for UI chip | `kb_sources` event gated on `kbSources.length > 0` |
| RunNow dispatches without error | ✅ PASS (code) / ⚠️ manual for toast | MCP slug resolution wrapped in graceful try/catch |
| No regression on Bedrock chat path | ✅ PASS (infra) / ⚠️ manual for E2E | App healthy, no startup errors |
| ECS healthy (fred-dev:186 stable, 1/1 running) | ✅ PASS | Container HEALTHY, 1/1, started 14:39 EDT |

---

## Issues Found

None — no bugs, crashes, or regressions detected.

**Pre-existing (not this deploy):**
- BraveSearch MCP 401 on ListTools startup (API key not active in this task def — pre-existing)
- Cloudflare Managed Challenge blocking headless browser access (pre-existing since ~2026-03-15)

---

## Recommendations

1. **Fred manual sign-off required** for runtime browser verification — open a conversation with prior history and confirm:
   - Resumption brief fires once on first load, not on each message send
   - No double-brief card on navigate-away / navigate-back within same harness session  
   - KB chip absent when searching returns no results
   - RunNow produces no error toast
   - Normal chat message streams correctly

2. BraveSearch 401 at startup should be investigated separately (pre-existing).

---

## Test Duration

~6 minutes (14:42–14:48 EDT)

---

_Trust nothing. Verify everything._
