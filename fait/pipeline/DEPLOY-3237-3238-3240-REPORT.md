# Deploy Report — ADO#3237 + ADO#3238 + ADO#3240 (Phase 2)

**Date:** 2026-05-10 23:41–23:48 EDT  
**Deployed by:** Rhodey (War Machine) — DevOps subagent  
**Commit:** `1f161cc7`

---

## Summary

Phase 2 combined deploy: Blazor + Harness. Three WIs shipped in a single deployment cycle.

---

## What Was Deployed

### ADO#3237 — Scheduled task: MS365 tools fail with auth error
- `ScheduledTaskBackgroundService.cs`: `EnabledMcpSlugs` now passed to `TurnRequest` for task execution context

### ADO#3238 — Resumption brief: inconsistent trigger, stale content, no contextual summary
- `ChatView.razor`: per-conversation resumption brief storage key, chat-switch state reset, `_isBriefStreaming` fix
- `harness-server.js`: `__resumption_brief__` handler generates Bedrock contextual summary

### ADO#3240 — MCP auth state: harness shows MS365/ADO unauthenticated; Brave invisible
- `Program.cs`: new `GET /api/internal/user-tokens/{userId}` endpoint + `POST /internal/mcp/brave` endpoint; dead `BraveSearchMcpAdapter.cs` deleted
- `harness-server.js`: `getUserTokens()` per-turn token fetch from Blazor; `brave`/`web_search` added to `MCP_TOOL_SPECS`; dead DB token functions removed
- ECS task def: added `INTERNAL_API_TOKEN` env var for securing the new internal endpoints

---

## ECR Images

| Service | Tag | Digest |
|---------|-----|--------|
| `fred-chat` | `1f161cc7` | `sha256:2f9973b5965ce03d95b95b17909cad1ddfd8c472f1a87f34069791f1c0fa14fc` |
| `fait-v2-agent-harness` | `1f161cc7` | `sha256:c6d4351a0717fafe5de6b510718d13ef622229794e363e5b45b5c3827bb71403` |

---

## Task Definition Revisions

| Task Def | Revision | Notes |
|----------|----------|-------|
| `fait-v2-agent-harness` | `:20` | Updated image → `1f161cc7` |
| `fred-dev` | `:181` | Updated image → `1f161cc7`; `Fargate__TaskDefinition` → harness `:20`; added `INTERNAL_API_TOKEN` |

> Note: `fred-dev` registered as `:181` (not `:179` as projected) — a prior revision had incremented the counter. Content and changes are as specified.

---

## ECS Service Status

```json
{
  "status": "ACTIVE",
  "taskDef": "arn:aws:ecs:us-east-1:742932328420:task-definition/fred-dev:181",
  "running": 1,
  "deployments": [
    {
      "status": "PRIMARY",
      "desired": 1,
      "running": 1
    }
  ]
}
```

✅ 1/1 RUNNING, single PRIMARY deployment. HEALTHY.

---

## CloudWatch

Log group `/ecs/fred-dev` shows clean startup. MCP servers responding normally:
- `devops` → 200 (ListTools OK)
- `brave` → 401 (expected — new `INTERNAL_API_TOKEN` auth gate working correctly)
- `m365` → 200 (ListTools OK)

---

## ADO Work Items

| WI | Title | Status |
|----|-------|--------|
| ADO#3237 | Scheduled task MS365 auth — EnabledMcpSlugs fix | ✅ Closed |
| ADO#3238 | Resumption brief — per-conv key + switch reset + contextual summary | ✅ Closed |
| ADO#3240 | MCP auth state — user-tokens endpoint + INTERNAL_API_TOKEN + Brave | ✅ Closed |

---

## Rollback Plan

| Service | Rollback Target |
|---------|----------------|
| Blazor (`fred-dev`) | `fred-dev:178` |
| Harness (`fait-v2-agent-harness`) | `fait-v2-agent-harness:19` |

```bash
# Rollback Blazor
aws ecs update-service --cluster fortress-tools-cluster --service fred-dev \
  --task-definition fred-dev:178 --region us-east-1

# Rollback harness TD (update Fargate__TaskDefinition back to :19 in Blazor TD)
```
