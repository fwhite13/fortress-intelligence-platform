# Active Pipeline Session Context — WI827

## Pipeline: WI827 — FfE S11: Formula Intelligence
## Started: 2026-03-17 ~01:33 EDT
## Risk: medium | Path: full

---

### [01:33] Maria — Critical flags for WI827 (SPRINT11-SPEC.md)

1. **`scratch cell clear()` in `finally` — NOT `try`**  
   If skipped, `__FAIT_SCRATCH__!A1` stays populated permanently.  
   `clearScratchCell()` must be in `finally`, not inside `try`.

2. **`__FAIT_SCRATCH__` sheet name** — exact string, underscores, all caps.

3. **`formulaBuilder.ts` is a new file** — all formula logic lives there. No logic in ChatPanel.

4. **`suggestionParser.ts` adds `formulaSpec`** — same pattern as `reportSpec` from WI826.

5. **`SlashCommandPicker.tsx` adds `/formula`** — alongside `/report`.

6. **Scratch sheet visibility**: `Excel.SheetVisibility.veryHidden` — pass as string `"VeryHidden"` when using `any` types.

7. **No Chart.js here** — chart.js is WI830 only. WI827 is formula-only.

8. **`worksheet.comments.add()` scoped in try/catch** — ExcelApi 1.10, non-fatal on duplicate.
