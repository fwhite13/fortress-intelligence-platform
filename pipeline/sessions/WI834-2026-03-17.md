# Active Pipeline Session Context — WI834

## Pipeline: WI834 — FAIT Cowork Sprint 2: Redis + approval gates + multi-type output + task history
## Started: 2026-03-17 ~12:20 EDT
## Risk: high | Path: full

---

### [12:20] Maria — Critical flags for WI834 (COWORK-SPRINT2-SPEC.md)

1. **TWO separate Redis clients** — `redis` for commands, `redisSub` for Pub/Sub subscribe.
   Calling `subscribe()` on the same client used for `hSet`/`get`/etc throws `"Connection in subscribe mode"` at runtime. Non-negotiable.

2. **User isolation** — `GET /tasks/:id` MUST check `meta.userId === authed.userId` → return 404 on mismatch (not 403). Missing check leaks any user's task data.

3. **Approval gate timeout = 5 min auto-reject** — `waitForApproval()` must set a deadline and auto-reject on timeout. Without this, a navigated-away task hangs indefinitely holding Agent SDK context in memory.

4. **Markdig `UseAdvancedExtensions()`** — `OutputPanel.razor` must use this pipeline. Without it, tables in .md output won't render as `<table>` elements.

5. **Replay log TTL on every `rPush`** — set TTL after every push, not just at stream creation. A 45-min task would expire a creation-time TTL before completion.

6. **`REDIS_URL` must be `rediss://`** (double-s, TLS). Never `redis://` in production.

7. **CSV rendering server-side only** — `lines.Take(101)` cap (1 header + 100 data rows). No JS parsers.

8. **Markdig added to CoworkWeb.csproj** — verify package reference correct (Markdig 0.37.0 per FAIT pattern).

9. **ElastiCache pre-deploy** — Rhodey must provision `cache.t4g.small` (TLS, `rediss://` URL) BEFORE ECS deploy. This is a new AWS resource.

10. **Sprint 1 follow-ups now in scope** — DataProtection key ring password fix + `COWORK_INTERNAL_SECRET` move to SSM are Sprint 2 prerequisites per deploy report.

### [13:27] Clint — WI834 post-CI-fix advisory
- `ensureConnected()` in taskStore.ts lacks promise-cache guard
- Two concurrent callers before `_connected=true` → duplicate client pairs (orphaned sockets)
- Not a correctness bug under normal load; patch in Sprint 3 with `_connectPromise ??=` pattern
- Non-blocking for WI834 deploy
