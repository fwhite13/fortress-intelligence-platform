# Active Pipeline Session Context — WI824

## Pipeline: WI824 — FfE S8: Named Range Registration
## Started: 2026-03-16 ~23:58 EDT
## Risk: medium | Path: full

---

### [23:58] Maria — Lessons from WI813–WI823

1. **FAIT wwwroot baked into Docker image** — CodeBuild rebuild required.
2. **public/manifest.xml is the deployed copy** — WI824 does not touch manifest files.
3. **fait-prod static image tag** — register fait-prod:26 (or next) during deploy.
4. **fip repo in sync** — pushed with every sprint.
5. **Standing approval active** — deploy immediately when Natasha passes.

---

### [23:58] Maria — Critical flags for WI824 (SPRINT8-SPEC.md)

1. `names.add()` address format: `=Sheet1!$A$1:$D$11` — `=` prefix + absolute `$` refs required.
   `toAbsoluteReference()` regex must handle multi-letter columns (AA, BC): `/([A-Z]+)(\d+)/g → '$$$$1$$$2'`.
2. Duplicate check: `getItemOrNullObject()` + `ctx.sync()` BEFORE `names.add()` — never add without checking first.
3. `namedItem.getRange()` for reference resolution (not `worksheet.getRange()`).
4. `syncRegistry()` guard: only sync if `listWorkbookNamedRanges()` succeeded with non-empty result.
5. Name prompt fires only in the cell-address branch of `handleWriteTableConfirm()` — not after `writeToTable()`.
