# Active Pipeline Session Context — WI835

## Pipeline: WI835 — FAIT Cowork Sprint 3: FORGE injection + persistent instructions + task queue
## Started: 2026-03-17 ~15:06 EDT
## Risk: high | Path: full

---

### [15:06] Maria — Critical flags for WI835 (COWORK-SPRINT3-SPEC.md)

1. **Atomic Lua script in `tryStartTask()`** — NOT separate GET + INCR. Race condition under concurrent load.
   Must be single `redis.eval()` call that checks count AND increments atomically.
   All keys in the script must hash to the same slot (use `{userId}` key prefix for cluster mode safety).

2. **`onTaskFinished()` floor at 0:**
   ```typescript
   const newCount = await redis.decr(countKey);
   if (newCount < 0) await redis.set(countKey, '0');
   ```
   Negative count = permanently blocked future starts for that user.

3. **`buildSearchForgeTool(userId, userEmail)` closure per-task:**
   Factory function returns a new tool object per invocation with userId/userEmail captured in closure.
   Module-level singleton = userId leaks across tasks.

4. **FORGE cache key MUST include userId:**
   `cowork:forge-cache:<userId>:<queryHash>` — NOT `cowork:forge-cache:<queryHash>`.
   Missing userId = User A's cached results returned to User B = data leak.

5. **Persistent instructions NOT in CloudWatch:**
   Log that instructions were loaded (and their length). Do NOT log the content.

6. **`TaskQueue.razor` polls at 10s interval — NOT 2s.**
   `new Timer(_ => ..., null, TimeSpan.Zero, TimeSpan.FromSeconds(10))`

7. **`TaskQueue.razor` must dispose Timer in `Dispose()`.**
   Dangling timer polls Redis every 10s after component is removed from DOM.

8. **Cancellation polling in `runner.ts` checks Redis AFTER each top-level Agent SDK message** —
   NOT inside `preToolCall` hook (cancellation mid-tool-call = undefined working dir state).

9. **No new npm packages.** No new AWS services.

10. **`ensureConnected()` promise-cache guard** (from WI834 Clint advisory):
    `_connectPromise ??= Promise.all([redis.connect(), redisSub.connect()])` pattern.
    Fix the concurrent double-connect risk from Sprint 2.

### From WI834 Sprint 2 deployment (context for Rhodey)
- ElastiCache: `rediss://master.cowork-redis.e3c7jk.use1.cache.amazonaws.com:6379`
- S3: `fip-cowork-workspaces`
- Current task defs: cowork-web:6, cowork-agent:6
- Next task defs: cowork-web:7 (if changed), cowork-agent:7
