# Active Pipeline Session Context — WI830

## Pipeline: WI830 — FfP Sprint 3: Data tables + template injection + chart-as-image
## Started: 2026-03-17 ~03:24 EDT
## Risk: medium | Path: full

---

### [03:24] Maria — Critical flags for WI830 (FFP-SPRINT3-SPEC.md)

1. **`chart.js` in `dependencies` not `devDependencies`** — runtime dependency, not build-only.

2. **Named imports only — NOT `chart.js/auto`**
   Required registers: `CategoryScale`, `LinearScale`, `BarController`, `BarElement`, `LineController`, `LineElement`, `PointElement`, `PieController`, `ArcElement`, `Title`, `Tooltip`, `Legend`.

3. **`responsive: false` + `animation: false`** required on all Chart.js configs — prevents layout thrash in Office iframe.

4. **`specificCellProperties` must be exactly `totalRows × columnCount` 2D array** — dimensions must match values array exactly.

5. **`fetchTemplateBase64` endpoint doesn't exist** — hardcode test template with `// TODO: DO NOT SHIP` comment. Clint MUST verify this comment exists before approving.

6. **Manifest bump**: `1.6` → `1.8` (WI830 — check spec for exact version).

7. **FfP repo**: `~/projects/fip/fait-for-powerpoint/` — NOT FfE.

8. **No changes to `~/projects/fait-for-excel/`**.
