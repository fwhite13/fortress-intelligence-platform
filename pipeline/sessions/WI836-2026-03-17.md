# Active Pipeline Session Context — WI836

## Pipeline: WI836 — Vendorply triage: folder search overrides DB match when existing emails found
## Started: 2026-03-17 ~16:13 EDT
## Risk: low | Path: full

---

### [16:13] Maria — Critical flags for WI836

1. **`searchMailbox()` in `graph-mail.ts` uses `/me/messages?$search`** (mailbox-wide, not folder-scoped)
   Pattern mirrors `searchFolder()` at line 518 but hits `/me/messages` endpoint.
   Must include `ConsistencyLevel: eventual` header (required for $search).
   ```typescript
   async searchMailbox(query: string, top = 25): Promise<GraphMessage[]>
   ```

2. **Classifier logic: DB match ≥0.80 → run mailbox search → override if folder concentration different**
   Current bug: line ~133 does `return` immediately on `confidence >= 0.80` before folder search runs.
   Fix: after DB match ≥0.80, call `searchMailbox(vendorName)`, analyze which member's folder existing emails concentrate in. If that member differs from DB match AND concentration confidence is high → override.
   DB match still wins if no mailbox evidence or ambiguous.

3. **`folder-searcher.ts` may need `searchMailbox` delegation** — check if FolderSearcher already wraps the graph-mail calls. If so, add `searchMailbox` there too as a pass-through.

4. **TypeScript strict mode** — vendorply uses strict TS. Return types must be explicit. No implicit `any`.

5. **Deploy = systemd restart, NOT ECS** — `sudo systemctl restart vendorply-triage.service` on SteamServer directly. No Docker, no CodeBuild, no ECR push.

6. **No new npm packages** — existing `axios`, `@microsoft/microsoft-graph-client` already available.

### Key code locations
- Classifier DB match + early return: `src/engine/classifier.ts` ~line 133
- searchFolder method: `src/services/graph-mail.ts` ~line 518
- FolderSearcher: `src/engine/folder-searcher.ts` ~line 50
- Existing $search pattern: `searchFolder` uses `$search: "${query}"` with ConsistencyLevel header
