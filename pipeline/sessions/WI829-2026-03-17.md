# Active Pipeline Session Context — WI829

## Pipeline: WI829 — FfP Sprint 2: Full slide scan + FORGE search panel + /notes + source tagging
## Started: 2026-03-17 ~02:43 EDT
## Risk: medium | Path: full

---

### [02:43] Maria — Critical flags for WI829 (FFP-SPRINT2-SPEC.md)

1. **`tags.add()` inside same `PowerPoint.run()` as text write** — proxy objects don't survive across run contexts.

2. **`isNotesCommand` detection**: `text.includes('ppt_notes_spec block')` — detect notes command from AI response.

3. **`stripAllSpecs()` replaces `stripNotesSpec()`** — `ChatPanel` display must strip notes spec blocks.

4. **Manifest bump**: `1.5` → `1.6` for WI829 (check spec for exact version).

5. **FfP repo**: `~/projects/fip/fait-for-powerpoint/` — NOT FfE.

6. **No changes to `~/projects/fait-for-excel/`** — FfE stays clean.

### [03:20] Rhodey — CodeBuild trigger finding (WI829 deploy)
- `fip-fait-build` does NOT auto-trigger from GitHub webhook on push to `fortress-intelligence-platform`
- Must be manually triggered via `aws codebuild start-build --project-name fip-fait-build`
- `--profile fortress-tools-deployer` returns AccessDenied for `codebuild:StartBuild`
- Use env vars from `.env.deployer` directly (without --profile) — this works
