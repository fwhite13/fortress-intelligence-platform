# Active Pipeline Session Context — WI844

## Pipeline: WI844 — FIRM v1: Fix 5 blocking gaps
## Started: 2026-03-17 ~15:46 EDT
## Risk: medium | Path: full

---

### [15:46] Maria — Critical flags for WI844 (FIRM-V1-SPEC.md)

From Jarvis + spec review:

1. **`ResolveFaitUserIdAsync` fires ONLY when `FaitUserId IS NULL`**
   Guard at top of `GetOrCreateUserAsync`:
   ```csharp
   if (user.FaitUserId != null) return user; // already resolved — skip
   ```
   Best-effort — never throw to the caller. Log warning on failure.

2. **`PushDocumentAsync` checks `firm_meeting_kb_pushes` BEFORE S3 upload**
   Query the table first. If row exists for `(meetingId, docType, kbScope)` → return early, do NOT re-upload, do NOT re-trigger Bedrock ingestion.

3. **`GetAudio()` returns `Redirect(url)` not `Ok(new { url })`**
   Must use `Redirect(presignedUrl)` so the browser follows directly to S3.
   Presigned URL must include `ResponseContentDisposition` header for download behavior.

4. **Old endpoints stay alive with `[Obsolete]` attribute**
   `push-transcript-to-kb` and `push-summary-to-kb` keep their full body.
   Just mark `[Obsolete("Use /push-to-kb instead")]`. Do NOT gut the methods.

5. **`Firm:SharedSecret` must match in BOTH ECS task defs**
   Rhodey MUST verify the secret value in firm-web AND fait-prod task defs are identical before declaring deploy complete. Mismatch = VpCallback 401s in production.

6. **Monorepo build** — docker build from `~/projects/fip/` root (not per-app dir)
   ```bash
   docker build -t "$IMAGE" -f ~/projects/fip/firm/Dockerfile.debian ~/projects/fip/
   ```

7. **`DatabaseInitializationService.extraTables`** — new `firm_meeting_kb_pushes` table MUST be added to the hardcoded CREATE TABLE loop. EF Core `CreateTablesAsync` is unreliable in containerized FIRM setup.

8. **No new packages** — spec uses existing dependencies only.

9. **`fip-tokens.css` 200** — mandatory check after every FIRM deploy.

10. **FIRM target**: `~/projects/fip/firm/src/FortressIntelligenceRM.Web/` — NOT FAIT, NOT cowork.

### From FIRM-V1-SPEC.md (928 lines)
- 1 new file: `Models/FirmMeetingKbPush.cs`
- 5 modified: `Services/MeetingService.cs`, `Controllers/MeetingsApiController.cs`, `Data/FirmDbContext.cs`, `Services/FirmKbService.cs`, `Data/DatabaseInitializationService.cs`
- Task 1: FaitUserId population (null-check guard + ResolveFaitUserIdAsync via Firm:SharedSecret)
- Task 2: Audio redirect fix (Redirect not Ok)
- Task 3: HttpClient base address fix in MeetingDetail.razor
- Task 4: firm_meeting_kb_pushes schema (new model + EF entity + extraTables entry)
- Task 5: PushDocumentAsync multi-KB service + push-to-kb API endpoint + UI changes

### From WI835 (just deployed — context for FIRM)
- fip HEAD: c4083da (Cowork Sprint 3)
- FAIT health clean, fip-tokens.css clean
- Current fait-prod task def: fait-prod:32
