# Active Pipeline Session Context — WI813

## Pipeline: WI813 — FfE: Fix Vite build foundation
## Started: 2026-03-16 ~11:35 EDT
## Risk: medium | Path: full

---

### [12:33] Rhodey — fip repo was 73 commits behind GitHub origin
The local `~/projects/fip` repo had never been pushed to GitHub. CodeBuild was building stale source. Rhodey pushed all 73 commits as part of this deploy. First CodeBuild run (before push) built wrong source. Future deploys should not have this problem now.

### [12:44] Rhodey — manifest is baked into Docker image, not served from mounted volume
Static file copy to wwwroot is NOT sufficient for manifest changes — the manifest is baked into the image at build time. Future manifest-only changes require a CodeBuild rebuild. This was confirmed during WI813 cycle 2 deploy: file copied + committed, live curl still showed old URLs, CodeBuild required to update.

### [12:36] Natasha — manifest.xml in wwwroot has stale URLs
`public/manifest.xml` was not updated by Tony — only root `manifest.xml` was. Vite copies `public/` to `dist/` during build, so the stale `public/manifest.xml` overwrote the correct source file in the deploy. The deployed manifest still points to bare `/excel-addin/` directory URLs. Root manifest.xml is correct. Fix: update `public/manifest.xml` to match root `manifest.xml`.

### [12:33] Rhodey — buildspec context mismatch (pre-existing from WI797)
`fait/buildspec.yml` used `docker build ... fait/` as build context, but WI797 updated the Dockerfile to use monorepo-relative paths (FipShared). Fix: `bfcc11c` changes buildspec to `docker build -f fait/Dockerfile .` (monorepo root context). Commit `bfcc11c` is in fip main.
