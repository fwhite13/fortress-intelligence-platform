# Active Pipeline Session Context — WI832

## Pipeline: WI832 — FAIT Cowork Sprint 1: Foundation + Task UI + Agent execution + SSE streaming
## Started: 2026-03-17 ~09:05 EDT
## Risk: high | Path: full

---

### [09:05] Maria — Critical flags for WI832 (COWORK-SPRINT1-SPEC.md)

1. **`AddDataProtection()` MUST have BOTH:**
   - `.SetApplicationName("FortressAI")` — must match FAIT/FIRM/FORMS exactly
   - `.DisableAutomaticKeyGeneration()` — FIP portal owns key creation
   Missing either one = shared .FortressAI.Session cookie broken for ALL FIP apps.

2. **`iframe sandbox = "allow-scripts"` ONLY** — NOT `allow-same-origin`. TaskPage.razor line ~724.

3. **`COWORK_INTERNAL_SECRET` must be from env var** — never hardcoded. `InternalTokenService.cs` reads `config["CoworkAgent:InternalSecret"]`; Node.js `auth.ts` reads `process.env.COWORK_INTERNAL_SECRET`.

4. **CloudWatch log group `/cowork/tasks` must exist before first deploy** — Rhodey creates it in pre-deploy infra step.

5. **Two new ECR repos needed:** `cowork-web` + `cowork-agent` — Rhodey creates in pre-deploy step.

6. **Two ECS services needed:** `cowork-web` + `cowork-agent` on fortress-tools-cluster — Rhodey creates task defs + services.

7. **Dockerfiles build from fip/ monorepo root** — `docker build -f cowork/Dockerfile.web .` (not from cowork/ subdir).

8. **FipShared modified** — `FipModule.cs` gets `Cowork` enum value. This affects the waffle menu in ALL FIP apps — Clint must verify the change is additive only.
