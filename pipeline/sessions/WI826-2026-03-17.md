# Active Pipeline Session Context — WI826

## Pipeline: WI826 — FfE S10: Multi-Sheet Report Generation
## Started: 2026-03-17 ~00:55 EDT
## Risk: medium | Path: full

---

### [00:55] Maria — Critical flags for WI826 (SPRINT10-SPEC.md)

1. **Em dash `—` (U+2014)** in sheet name — NOT hyphen. Tony must paste the actual character into the CC brief.
2. **`report_spec.chartSpec.dataRange` override BEFORE `insertChart()`** — if left as source data range, chart reads from wrong location on report sheet.
3. **`setFaitWriting(true/false)` wraps the ENTIRE `createReportSheet()` in a `finally`** — covers all three `Excel.run()` calls inside.
4. **`range.merge(false)`** — `false` = merge all into one cell (correct for title/summary). Not `merge(true)`.
5. **`existingSheet.isNullObject` check after `ctx.sync()`** — sync boundary rule.
6. **`handleCreateReportSheet()` checks `reportLoading`** — prevent double-click.
7. **Two-phase flow**: FAIT analysis first (chat thread), THEN "Create Report Sheet" button.
