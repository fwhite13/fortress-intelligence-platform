# Active Pipeline Session Context — WI825

## Pipeline: WI825 — FfE S9: Reactive Workbook Watching
## Started: 2026-03-17 ~00:31 EDT
## Risk: medium | Path: full

---

### [00:31] Maria — Critical flags for WI825 (SPRINT9-SPEC.md)

1. `setFaitWriting(false)` in `finally` — not `try`. Failed write must still clear flag or watch mode stays permanently suppressed.
2. `handleWatchChange` must NOT be `async` — proxy becomes invalid after sync tick. Use `setTimeout(() => triggerWatchAnalysis(), 0)` as the async boundary.
3. `registerWatchHandler()` return value stored in `eventHandlerRef.current` — otherwise `stopWatching()` can't deregister.
4. `clearTimeout` before `unregisterWatchHandler` in `stopWatching()`.
5. `ctx.runtime.enableEvents = false` BEFORE `range.values = ...` assignment.
6. Both `applySuggestions()` AND `applySingleSuggestion()` in WriteSuggestionsDialog need the `setFaitWriting` wrap.
7. `watchPulse` animation name — check for conflict with `blink` keyframe in MessageBubble.
8. ExcelApi 1.13 bump required in manifest.xml and manifest.local.xml — check both.
