# Deploy Report: WI906 — Sprint 6 v3 (EF Column Mapping Fix)

**Date:** 2026-03-19 / 2026-03-20 UTC  
**Deployer:** War Machine (James Rhodes) — `devops`  
**Commit:** `ca5b792`  
**Build ID:** `fip-famos-build:8afe05a4-a1f1-48ab-bc98-c1411c8b2211`

---

## Change Summary

EF `HasColumnName()` mappings added for snake_case columns in:
- `contacts` entity
- `opportunity_documents` entity

Full Docker rebuild required (EF model change). Previous deploys (v1, v2) failed /opportunity with HTTP 500 due to missing column mappings.

---

## Pre-Deploy Snapshot

| Item | Value |
|------|-------|
| Task Definition | `famos-dev:3` |
| Rollback Target | `famos-dev:3` (same, force new deployment) |
| ECR Image Tag | `dev-latest` (overwrite strategy) |
| /opportunity status (pre) | 500 |

---

## Deploy Steps

| Step | Status | Notes |
|------|--------|-------|
| ADO comment posted (DEPLOY v3) | ✅ DONE | Comment ID 726501 |
| CodeBuild triggered | ✅ DONE | `fip-famos-build:8afe05a4-a1f1-48ab-bc98-c1411c8b2211` |
| Docker build | ✅ SUCCEEDED | ~64s build time |
| ECR push (`dev-latest`) | ✅ DONE | `742932328420.dkr.ecr.us-east-1.amazonaws.com/famos-web:dev-latest` |
| ECR push (`latest`) | ✅ DONE | Mirror tag |
| ECS force-new-deployment | ✅ DONE | CodeBuild POST_BUILD phase |
| ECS stabilization | ✅ DONE | 7 polls (~3.5 min); rollout=COMPLETED |
| Health checks | ✅ ALL PASS | See below |
| Startup log check | ✅ CLEAN | No unknown column / unhandled exception errors |

---

## Health Check Results

| Endpoint | Expected | Actual | Status |
|----------|----------|--------|--------|
| Root (`/`) | 200/302 | **200** | ✅ PASS |
| `/health` | 200 | **200** | ✅ PASS |
| `/_blazor` | 302 | **302** | ✅ PASS |
| `/_content/FipShared/css/fip-tokens.css` | 200 | **200** | ✅ PASS |
| `/opportunity/0b57562c-...` (X-QA-Bypass) | 200 | **200** | ✅ PASS |

**TaskDef:** `arn:aws:ecs:us-east-1:742932328420:task-definition/famos-dev:3`

---

## Startup Log Analysis

**Log stream:** `famos-web/famos-web/cdab8416841a40aea51f02fc69081378`  
**Result:** ✅ CLEAN — No `unknown column` or `unhandled exception` errors detected.

---

## Rollback Plan

If health checks degrade post-verification:

```bash
source ~/projects/ai/projects/fortress_tools/.env.deployer
aws ecs update-service \
  --cluster fortress-tools-cluster \
  --service famos-dev \
  --task-definition famos-dev:3 \
  --force-new-deployment \
  --region us-east-1
```

> Note: Rollback target is the same task def (`famos-dev:3`) since image is tag-based (`dev-latest`). A true rollback would require re-deploying from the previous commit tag or ECR image digest.

---

## Outcome

**✅ DEPLOY v3 SUCCEEDED**

The EF `HasColumnName()` fix for contacts and opportunity_documents snake_case columns is live. `/opportunity` endpoint returned 200 (was 500 on v1/v2). Startup logs clean.

**Natasha to re-verify.**

---

*Report generated by War Machine (James Rhodes) — devops agent*
