version: "3.8"

services:
  portal:
    build:
      context: .
      dockerfile: src/FortressPortal.Web/Dockerfile
    ports:
      - "3334:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - UseStubAuth=true
    restart: unless-stopped
